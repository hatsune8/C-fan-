<%= form_with model:@Item, url: items_path, method: :post, local:true do |f| %>
title
<%= f.text_field :title %>
opinion
<%= f.text_area :opinion %>

<%= f.submit '投稿', class: 'btn btn-primary' %>

<% end %>